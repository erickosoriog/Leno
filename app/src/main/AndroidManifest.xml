<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.leno">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_leno"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_leno_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activitys.NoteLayout" />
        <activity
            android:name=".ui.activitys.ForgotPassword"
            android:label="@string/forgot_password"
            android:parentActivityName="app.leno.ui.activitys.Login" />
        <activity
            android:name=".ui.activitys.Register"
            android:label=""
            android:parentActivityName="app.leno.ui.activitys.Welcome" />
        <activity
            android:name=".ui.activitys.Login"
            android:label=""
            android:parentActivityName="app.leno.ui.activitys.Welcome" />
        <activity android:name=".ui.activitys.Welcome" />
        <activity android:name=".startApp.StateCurrentUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activitys.MainActivity"
            android:configChanges="screenSize|orientation"
            android:windowSoftInputMode="adjustNothing" />
    </application>

</manifest>